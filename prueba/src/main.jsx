import React from 'react'
import ReactDOM from 'react-dom/client'
import { PokemonsApp } from './PokemonsApp.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PokemonsApp />
  </React.StrictMode>,
)
